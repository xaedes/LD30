define(['ash'], function (Ash) {
    var Squad = Ash.Class.extend({
        constructor: function () {
        }
    });

    return Squad;
});

// todo : don't forget to add this component to Components.js